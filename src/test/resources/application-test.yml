spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop

# React frontend configuration for tests
react:
  frontend:
    url: http://localhost:5173
    timeout: 30

logging:
  level:
    com.example.demo1: INFO
    org.springframework: WARN